#version 450 core

//in vec4 fragPosition;

//out vec4 FragColor;

void main()
{
    // Output the depth value of the fragment
    //gl_FragDepth = fragPosition.z;
	
	
	//vec3 shadowColor = vec3(0.0, 0.0, 1.0);
	//FragColor = fragPosition.z * vec4(shadowColor, 1.0);
}